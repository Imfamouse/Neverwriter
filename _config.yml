title: Articles Archive
description: Markdown-архив статей
theme: jekyll-theme-cayman

# Настройка коллекции статей
collections:
  articles:
    output: true
    permalink: /:collection/:name/

# Значения по умолчанию
defaults:
  - scope:
      path: "articles"
      type: "articles"
    values:
      layout: "article"
